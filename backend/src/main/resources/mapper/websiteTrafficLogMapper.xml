<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="priv.backend.mapper.WebsiteTrafficLogMapper">
    <!--SQL-->

    <!--SQL-->

    <!--Map-->
    <resultMap id="getAllMap" type="RespWebsiteTrafficLogVO" >
        <result property="pageUrl" column="page_url" />
        <result property="ip" column="visitor_ip" />
        <result property="timestamp" column="timestamp" />
        <result property="userAgent" column="user_agent" />
        <result property="osName" column="os_name" />
        <result property="deviceType" column="device_type" />
        <result property="browserType" column="browser_type" />
        <result property="browserVersion" column="browser_version" />
        <result property="region" column="region" />
        <result property="network" column="network" />
    </resultMap>
    <!--Map-->

    <!--Select-->
    <select id="getAll" resultMap="getAllMap">
        SELECT
            page_url,
            visitor_ip,
            timestamp,
            user_agent,
            os_name,
            device_type,
            browser_type,
            browser_version,
            country,
            region,
            network
        FROM website_traffic_log
    </select>
    <!--Select-->

    <!--Update-->

    <!--Update-->

    <!--Insert-->

    <!--Insert-->

    <!--Delete-->

    <!--Delete-->

</mapper>