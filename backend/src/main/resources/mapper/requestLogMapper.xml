<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="priv.backend.mapper.RequestLogMapper">
    <!--SQL-->

    <!--SQL-->

    <!--Map-->

    <!--Map-->

    <!--Select-->
    <select id="getAll" resultType="RespRequestLogVO">
        SELECT
            url,
            method,
            ip,
            position,
            parameter,
            response_value,
            time_consuming,
            request_time,
            country,
            province,
            region,
            network,
            device_type,
            os_name,
            browser_type,
            browser_version
        FROM request_log
    </select>
    <!--Select-->

    <!--Update-->

    <!--Update-->

    <!--Insert-->

    <!--Insert-->

    <!--Delete-->

    <!--Delete-->

</mapper>