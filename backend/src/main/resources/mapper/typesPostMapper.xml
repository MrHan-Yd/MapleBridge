<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="priv.backend.mapper.TypesPostMapper">
    <!--SQL-->

    <!--SQL-->

    <!--Map-->

    <!--Map-->

    <!--Select-->
        <!--分页查询所有帖子类型-->
        <select id="getPageTypesPost" resultType="TypesPost">
            select
                type_id,
                type_name,
                description,
                create_id,
                create_time,
                update_id,
                update_time
            from
                types_post
        </select>
        <!--分页查询所有帖子类型-->
        <!--查询所有帖子类型-->
        <select id="getAllTypesPost" resultType="RespTypesPostSelectVO">
            select
                type_id,
                type_name
            from
                types_post
        </select>
        <!--查询所有帖子类型-->
        <!--根据ID查询对应的类型ID和名称-->
        <select id="getTypesPostById" resultType="RespTypesPostSelectVO" >
            select
                type_id,
                type_name
            from
                types_post
            where type_id = #{typeId}
        </select>
        <!--根据ID查询对应的类型ID和名称-->
    <!--Select-->

    <!--Update-->

    <!--Update-->

    <!--Insert-->

    <!--Insert-->

    <!--Delete-->

    <!--Delete-->

</mapper>