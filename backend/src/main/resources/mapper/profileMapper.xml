<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="priv.backend.mapper.ProfileMapper">
    <!--SQL-->

    <!--SQL-->

    <!--Map-->

    <!--Map-->

    <!--Select-->
        <!--根据个人信息唯一标识查询对应数据-->
        <select id="getProFileByUserId" resultType="Profile" parameterType="String" >
            select
                profile_id,
                user_id,
                last_name,
                first_name,
                region,
                location,
                graduation_department,
                graduation_year
            from profile
            where user_id = #{userId}
        </select>
        <!--根据个人信息唯一标识查询对应数据-->
    <!--Select-->

    <!--Update-->

    <!--Update-->

    <!--Insert-->

    <!--Insert-->

    <!--Delete-->

    <!--Delete-->

</mapper>